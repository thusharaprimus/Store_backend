
var config = {
	db: {
		host: 'localhost',
		user: 'root',
		password: '', //Insert the root password of the db
		database: 'store'
	},
	secret: 'INSERT YOUR JWT SECRET HERE'
}

module.exports = config;
